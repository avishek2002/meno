# Branching diagram notes

Fixture stand-in for the learner's own material. In a real course this would be Sam's actual
diagram and notes; here it exists only so `module.yml` has a second `source_type: user` record
to point at.
