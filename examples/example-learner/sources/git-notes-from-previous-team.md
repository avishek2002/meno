# Git notes from a previous team

Fixture stand-in for the learner's own material. In a real course this would be Sam's actual
notes; here it exists only so `module.yml` has a `source_type: user` record to point at.
