# notes-cli (fixture)

A small command-line note-taking tool, part of the workspace-scan committed fixture tree
(`examples/workspace-fixture/`). It intentionally carries no `tests/` directory and no
continuous-integration configuration, so a scan of this fixture demonstrates marker absence:
the strongest gap signal this subsystem has.
