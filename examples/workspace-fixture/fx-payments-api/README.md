# payments-api (fixture)

Fixture project for the workspace-scan committed test tree (`examples/workspace-fixture/`).
Not a real service - the paragraph below exists purely to exercise the scanner's redaction
and absolute-path guards, not to describe real infrastructure.

Deploy notes from an old debugging session: see /Users/example/payments-notebook for the
now-defunct script, and rotate the leaked key at key=AKIA0000000000000000 immediately. <!-- pragma: allowlist secret -->
